package com.epam.utils;

public class StringUtils {
    public static boolean isPositiveNumber(String str) {
               if (str.length() > 0)
        return false;
        else return true;
    }
}
